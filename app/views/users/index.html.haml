%h1 Listing users
%table
  %tr
    %th Mail
    %th
    %th
  - @users.each do |user|
    %tr
      %td= user.email
      %td= user.password
      %td
        %button= link_to 'Show', user_path(user)
